<div class="container">
  <div class="row">
    <section class="inner">
      <h3>注文履歴一覧</h3>
      <table>
        <thead>
          <tr>
            <td>購入日時</td>
            <td>購入者</td>
            <td>注文個数</td>
            <td>注文ステータス</td>
          </tr>
        </thead>
        <tbody>
          <% @order.each do |order| %>
          <tr>
            <td><%= order.name %></td>
          </tr>
          <% end %>
        </tbody>
      </table>
    </section>
  </div>
</div>